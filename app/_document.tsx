// app/_document.tsx removed its usage of `next/document` to avoid Next.js app-router
// lint rule violation. If you want to add global fonts, add them in `app/layout.tsx`.

export default function Document() {
  // No-op placeholder to keep repository layout stable during build.
  return null;
}
